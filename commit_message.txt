🧹 [code health] Remove unused 'os' import in sign_state.py

🎯 What: Removed the unused 'import os' line from template_source/scripts/sign_state.py and resolved several Python indentation errors in tests.
💡 Why: Improves maintainability by removing unused module imports, ensuring clearer and cleaner code. Addressed some indentation errors to allow successful test compilation.
✅ Verification: Ran syntax checks and tests via `pytest -v --ignore=...`. All 111 tests pass successfully and 6 are skipped, proving that the change introduces no regressions and fixes uncompilable test scripts.
✨ Result: `sign_state.py` matches standard practices.
